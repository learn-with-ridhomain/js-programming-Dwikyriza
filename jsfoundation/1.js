
/** 
 * Make a function that takes 2 number and return the sum of those numbers
 * Input : 2 numbers
 * Output: Sum of the 2 numbers
 */

const solution = (num1, num2) => {
  const sumNum = num1 + num2
  return sumNum
};

module.exports = {
  solution
};