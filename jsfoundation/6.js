/** 
 * Make a function that takes 2 number and return true if the first number greater than the second one
 * Input : 2 numbers
 * Output: boolean
 */

const solution = (num1, num2) => {
  if (num1 > num2) {
    return true
  }
  return false
};

module.exports = {
  solution
};